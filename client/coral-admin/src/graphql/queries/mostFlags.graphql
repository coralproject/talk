query Metrics ($from: Date!, $to: Date!) {
  metrics(from: $from, to: $to) {
    id
    title
    url
    commentCount
    author
    created_at
    action_summaries {
      actionCount
      actionableItemCount
    }
  }
}
