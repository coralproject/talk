query Users {
  usersFlagged {
    id
    username
    status
    roles
    actions{
      id
      reason
      user {
        username
      }
    }
    action_summaries {
      count
      reason
    }
  }
}
