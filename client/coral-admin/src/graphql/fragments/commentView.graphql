fragment commentView on Comment {
    id
    body
    created_at
    status
    user {
        id
        name: username
        status
    }
    asset {
      id
      title
    }
    actions {
      ... on FlagAction {
        reason
        message
        user {
          username
        }
      }
    }
}
