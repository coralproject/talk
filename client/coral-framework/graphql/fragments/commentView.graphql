#import "../fragments/actionSummaryView.graphql"

fragment commentView on Comment {
    id
    body
    created_at
    status
    user {
        id
        name: displayName
    }
    action_summaries {
      ...actionSummaryView
    }
}
