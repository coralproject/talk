mutation RemoveCommentTag ($id: ID!, $tag: String!) {
  removeCommentTag(id:$id, tag:$tag) {
    comment {
      id
      tags {
        tag {
          name
        }
      }
    }
    errors {
      translation_key
    }
  }
}
