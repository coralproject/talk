import { NotificationCategory } from "./category";
import { reply } from "./reply";

/**
 * categories stores all the notification categories in a flat list.
 */
const categories: NotificationCategory[] = [...reply];

export default categories;
