// TODO: This is very much not started.
// It should use pym to render the iFrame,
// and accepts some parameters like assetID
// assetURL, commentID to build the query..

export default class StreamControl {
  constructor(target: HTMLElement) {
    target.innerHTML = "Hello World!";
  }
}
