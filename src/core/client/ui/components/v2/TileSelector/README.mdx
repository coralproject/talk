---
name: TileSelector
menu: UI Kit v2
---

import { Playground } from "docz";
import { useState } from "react";

import {
  AlertTriangleIcon,
  CalendarInformationIcon,
  ConversationChatTextIcon,
  MessagesBubbleSquareIcon,
  QuestionHelpMessageIcon,
  SvgIcon,
} from "coral-ui/components/icons";

import TileSelector from "./TileSelector";
import TileOption from "./TileOption";
import HorizontalGutter from "../HorizontalGutter";

# TileSelector

## Basic Use

<Playground>
  {() => {
    const [value, setValue] = useState(null);
    return (
      <TileSelector
        id="icon"
        name="icon"
        onChange={(val) => setValue(val)}
        value={value}
      >
        <TileOption value="question_answer">
          <SvgIcon Icon={ConversationChatTextIcon} />
        </TileOption>
        <TileOption value="today">
          <SvgIcon Icon={CalendarInformationIcon} />
        </TileOption>
        <TileOption value="help_outline">
          <SvgIcon Icon={QuestionHelpMessageIcon} />
        </TileOption>
        <TileOption value="warning">
          <SvgIcon Icon={AlertTriangleIcon} />
        </TileOption>
        <TileOption value="chat_bubble_outline">
          <SvgIcon Icon={MessagesBubbleSquareIcon} />
        </TileOption>
        <TileOption value="">No Icon</TileOption>
      </TileSelector>
    );
  }}
</Playground>
