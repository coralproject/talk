enum View {
  SIGN_UP
  SIGN_IN
  FORGOT_PASSWORD
  RESET_PASSWORD
}

type Local {
  authToken: String
  authExp: Int
  authJTI: String
  loggedIn: Boolean!
  view: View!
}

extend type Query {
  local: Local!
}
