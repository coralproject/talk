# Extend graph with local types
type Network {
  isOffline: Boolean!
}

type Local {
  network: Network!
  assetID: String
  commentID: String
  host: String
  origin: String
}

extend type Query {
  local: Local!
}
