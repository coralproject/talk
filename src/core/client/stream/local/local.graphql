# Extend graph with local types
type Network {
  isOffline: Boolean!
}

type Local {
  network: Network!
  assetID: String
  commentID: String
  assetURL: String
}

extend type Query {
  local: Local!
}
