# Extend graph with local types
type Network {
  isOffline: Boolean!
}

enum View {
  SIGN_UP
  SIGN_IN
  FORGOT_PASSWORD
}

type AuthPopup {
  open: Boolean!
  focus: Boolean!
  view: View
}

type Local {
  network: Network!
  assetID: String
  assetURL: String
  commentID: String
  authPopup: AuthPopup!
}

extend type Query {
  local: Local!
}
