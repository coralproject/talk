# Extend graph with local types
type Network {
  isOffline: Boolean!
}

type Local {
  network: Network!
  authToken: String
  authExp: Number
  authJTI: String
  loggedIn: Boolean!
}

extend type Query {
  local: Local!
}
